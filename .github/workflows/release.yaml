name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  packages: write

jobs:
  # ===========================================================================
  # Step 1: Calculate version and create draft release
  # ===========================================================================
  create-release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      release_created: ${{ steps.create_release.outputs.release_created }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Calculate Version
        id: version
        run: |
          NEW_VERSION=$(make -s version)
          echo "New version: $NEW_VERSION"
          echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: Create Draft Release
        id: create_release
        run: |
          gh release create "${{ steps.version.outputs.version }}" \
            --title "Release ${{ steps.version.outputs.version }}" \
            --draft \
            --notes "Raikiri ${{ steps.version.outputs.version }}" \
            --target ${{ github.sha }}
          echo "release_created=true" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ github.token }}

  # ===========================================================================
  # Step 2a: Build and push Docker image (parallel)
  # ===========================================================================
  docker:
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: tanq16
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            tanq16/raikiri:latest
            tanq16/raikiri:${{ needs.create-release.outputs.version }}

  # ===========================================================================
  # Step 2b: Build binaries (parallel matrix)
  # ===========================================================================
  binaries:
    needs: create-release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux, darwin]
        arch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'

      - name: Download Assets
        run: make assets

      - name: Build Binary
        run: make build-for GOOS=${{ matrix.os }} GOARCH=${{ matrix.arch }} VERSION=${{ needs.create-release.outputs.version }}

      - name: Upload Release Asset
        run: |
          gh release upload "${{ needs.create-release.outputs.version }}" \
            "raikiri-${{ matrix.os }}-${{ matrix.arch }}" \
            --clobber
        env:
          GH_TOKEN: ${{ github.token }}

  # ===========================================================================
  # Step 2c: Build Android APK (parallel)
  # ===========================================================================
  android:
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Java 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '9.4.1'

      - name: Decode Keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.p12

      - name: Build Release APK
        working-directory: android
        env:
          KEYSTORE_PATH: keystore.p12
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: gradle :app:assembleRelease -PappVersion=${{ needs.create-release.outputs.version }}

      - name: Upload APK to Release
        run: |
          gh release upload "${{ needs.create-release.outputs.version }}" \
            "android/app/build/outputs/apk/release/app-release.apk#raikiri-android.apk" \
            --clobber
        env:
          GH_TOKEN: ${{ github.token }}

  # ===========================================================================
  # Step 3: Publish release
  # ===========================================================================
  publish:
    needs: [create-release, docker, binaries, android]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Publish Release
        run: gh release edit "${{ needs.create-release.outputs.version }}" --draft=false
        env:
          GH_TOKEN: ${{ github.token }}

  # ===========================================================================
  # Cleanup on failure
  # ===========================================================================
  cleanup-on-failure:
    needs: [create-release, docker, binaries, android, publish]
    if: always() && needs.create-release.outputs.release_created == 'true' && (needs.docker.result != 'success' || needs.binaries.result != 'success' || needs.android.result != 'success' || needs.publish.result != 'success')
    runs-on: ubuntu-latest
    steps:
      - name: Delete Draft Release
        run: |
          echo "Cleaning up draft release due to workflow failure"
          gh release delete "${{ needs.create-release.outputs.version }}" --repo "$GITHUB_REPOSITORY" --yes
        env:
          GH_TOKEN: ${{ github.token }}
